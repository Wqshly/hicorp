<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hicorp.segment.mapper.DeviceInfoMapper">
    <select id="countNumber" resultType="java.lang.Integer">
        select count(*) from device_info where name = #{name, jdbcType=VARCHAR}
    </select>
    <select id="selectNameById" resultType="java.lang.String">
        select name from device_info where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectByName" resultType="com.hicorp.segment.pojo.DeviceInfo">
        select *
        from device_info
        where name = #{name, jdbcType=VARCHAR}
    </select>
</mapper>